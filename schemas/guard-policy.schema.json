{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://raw.githubusercontent.com/saifeldeen911/actionfence/main/schemas/guard-policy.schema.json",
  "title": "Guard Policy",
  "description": "Policy file for ActionFence — defines what AI agents are allowed to do.",
  "type": "object",
  "required": ["service", "version", "actions"],
  "additionalProperties": false,
  "properties": {
    "$schema": {
      "type": "string",
      "description": "Optional JSON Schema reference for editor support."
    },
    "service": {
      "type": "string",
      "minLength": 1,
      "description": "Name of the service this policy belongs to."
    },
    "version": {
      "type": "string",
      "minLength": 1,
      "description": "Version string for this policy."
    },
    "default_rule": {
      "type": "string",
      "enum": ["allow", "deny"],
      "default": "deny",
      "description": "Default behavior for actions not listed in 'actions'. Defaults to 'deny' when omitted."
    },
    "actions": {
      "type": "object",
      "description": "Map of action names to their permission rules.",
      "minProperties": 1,
      "additionalProperties": {
        "type": "object",
        "required": ["allowed"],
        "additionalProperties": false,
        "properties": {
          "allowed": {
            "type": "boolean",
            "description": "Whether this action is permitted."
          },
          "identity": {
            "type": "string",
            "enum": ["any", "token", "verified"],
            "description": "Minimum identity tier required. Defaults to 'any' if omitted."
          },
          "max_spend": {
            "type": "number",
            "minimum": 0,
            "description": "Maximum spend amount allowed for this action in major units."
          },
          "currency": {
            "type": "string",
            "pattern": "^[A-Z]{3}$",
            "description": "ISO 4217 currency code for max_spend, such as USD."
          },
          "requires_human_approval": {
            "type": "boolean",
            "description": "Whether human approval is required before execution."
          }
        }
      }
    },
    "rate_limits": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "requests_per_minute": {
          "type": "integer",
          "minimum": 0,
          "description": "Maximum requests per minute per agent. Set to 0 for unlimited."
        },
        "transactions_per_day": {
          "type": "integer",
          "minimum": 0,
          "description": "Maximum transactions per day per agent. Set to 0 for unlimited."
        }
      }
    },
    "spend_limits": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "session_max": {
          "type": "number",
          "minimum": 0,
          "description": "Maximum projected session spend per agent in major units."
        },
        "daily_max": {
          "type": "number",
          "minimum": 0,
          "description": "Maximum projected UTC-day spend per agent in major units."
        },
        "window": {
          "type": "object",
          "description": "Rolling-window spend cap. Blocks when cumulative spend within duration_minutes exceeds max_amount.",
          "additionalProperties": false,
          "properties": {
            "max_amount": {
              "type": "number",
              "minimum": 0,
              "description": "Maximum spend allowed within the rolling window in major units."
            },
            "duration_minutes": {
              "type": "integer",
              "minimum": 1,
              "description": "Duration of the rolling window in minutes."
            }
          },
          "required": ["max_amount", "duration_minutes"]
        },
        "currency": {
          "type": "string",
          "pattern": "^[A-Z]{3}$",
          "description": "ISO 4217 currency code for spend reporting."
        }
      },
      "dependencies": {
        "session_max": ["currency"],
        "daily_max": ["currency"]
      }
    },
    "regulations": {
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1
      },
      "description": "Regulatory frameworks (stored only, not enforced in v1)."
    }
  }
}
